version: "3.8"

services:
  webhook:
    build: .
    ports:
      - "8028:8000"
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - uploads_data:/app/uploads
    restart: unless-stopped

volumes:
  uploads_data:
